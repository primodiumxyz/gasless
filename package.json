{
  "name": "gaseless-server",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "deploy:contracts:local": "pnpm -filter contracts deploy:local",
    "dev": "pnpm -r dev",
    "dev:contracts": "pnpm -filter contracts dev",
    "dev:server": "pnpm -filter server dev",
    "lint": "pnpm eslint --cache --fix .",
    "prepare": "husky && husky install",
    "sort-package-json": "npx sort-package-json package.json 'packages/*/package.json'",
    "test": "pnpm -r test",
    "test:server": "pnpm -filter server test",
    "test:server:ui": "pnpm -filter server test:ui",
    "test:server:watch": "pnpm -filter server test:watch",
    "test:watch": "pnpm -r test:watch"
  },
  "lint-staged": {
    "*.{ts,tsx,css,md,mdx,sol}": "prettier --write",
    "package.json": "pnpm sort-package-json"
  },
  "devDependencies": {
    "@eslint/js": "^9.6.0",
    "@ianvs/prettier-plugin-sort-imports": "^4.3.0",
    "@types/eslint__js": "^8.42.3",
    "eslint": "^9.6.0",
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^3.3.2",
    "typescript": "^5.5.3",
    "typescript-eslint": "^7.16.0"
  }
}
